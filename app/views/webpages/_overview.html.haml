#overview_tab.tab-pane.fade.in.active
  .table-responsive
    %table.table.table-bordered.table-hover
      %tr
        %th IP address
        %td= webpage.primary_ip
      - if webpage.location.to_s != ''
        %tr
          %th IP location
          %td= webpage.location.to_s
      %tr
        %th Redirect count
        %td= webpage_response.redirect_count
      - if webpage_response.redirect_count > 0
        %tr
          %th Redirects
          %td
            %ol
              - webpage_response.webpage_redirects.each do |redirect|
                %li= redirect.url
